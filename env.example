# Postgres credentials
POSTGRES_DB=notevault
POSTGRES_USER=notevault
POSTGRES_PASSWORD=changeme

# Volumes mounts
DB_DATA_PATH=./data/postgres
APP_DATA_PATH=./data/chromium

# Host port the app is exposed on (container always uses 5959 internally)
APP_PORT=5959
